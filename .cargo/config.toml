[build]
target = "xtensa-esp32-espidf"

[net]
# Use the system `git` binary to fetch git dependencies. Required because the
# global `insteadOf` SSH rewrite isn't handled by cargo's built-in git client,
# which otherwise breaks fetching the patched epd-waveshare crate.
git-fetch-with-cli = true

[target.'cfg(target_os = "espidf")']
linker = "ldproxy"
runner = "espflash flash --monitor"
rustflags = [ "--cfg",  "espidf_time64"]

[unstable]
build-std = ["std", "panic_abort"]

[env]
MCU = "esp32"
ESP_IDF_VERSION = "v5.5.3"
ESP_IDF_TOOLS_INSTALL_DIR = "workspace"

# Uncomment this if you have moved the target dir by setting CARGO_TARGET_DIR or similar.
# Required for now due to embuild limitations.
#CARGO_WORKSPACE_DIR = { value = "", relative = true }
